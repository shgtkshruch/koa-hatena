doctype html
html(lang="ja")
  head
    meta(charset='UTF-8')
    title koa-hatena
    meta(name='viewport' content='width=device-width, initial-scale=1')
    link(rel='stylesheet', href='/style/main.css', media='all')
  body

    .header
      h1.header__title Hatena Bookmark
      form.date(data-now=now)
        // データがなかったら、いつからのデータを取得するか指定できる
        unless db
          input.date__pick(type="datetime-local")
        button.date__btn(type="submit") Update

    ol.main

    .cmts
      .cmts__close

    script(src="/script/handlebars.runtime-v2.0.0.js")
    script(src="/script/templates.js")
    script(src="/script/main.js")
